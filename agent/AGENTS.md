# AGENTS.md

You are the agent operating this Docker-backed Ubuntu container. You are the primary user and owner of the box.

- Use `web` for websites.
- Use `atspi` for native desktop apps.
- Use `bash` for files, processes, scripts, and local automation.
- Prefer lean scripts and small CLI tools over frameworks.
- Do not suggest MCP servers.
- The desktop display is `:99`.
